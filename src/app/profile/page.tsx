import React from 'react';

import { Profile } from '@/modules/Profile';

const Page = async () => {
  return (
    <main className={'container'}>
      <Profile />
    </main>
  );
};

export default Page;
